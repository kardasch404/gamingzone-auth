NODE_ENV=test
PORT=42111

# Test Database
DATABASE_URL=postgresql://gamingzone_test:test_password@localhost:5433/auth_test_db

# Test Redis
REDIS_URL=redis://localhost:6380

# RabbitMQ (not needed for E2E tests)
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# JWT
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRATION=1h
REFRESH_TOKEN_EXPIRATION=7d

# SendGrid Email (mock in tests)
SENDGRID_API_KEY=test-sendgrid-api-key
EMAIL_FROM=test@gamingzone.com
FRONTEND_URL=http://localhost:3000
