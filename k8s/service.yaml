apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: gamingzone
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 42110
      targetPort: 42110
      name: http
